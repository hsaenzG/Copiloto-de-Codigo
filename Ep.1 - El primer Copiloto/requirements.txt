# Core agent orchestration
strands-agents>=0.1.0
strands-agents-tools>=0.1.0

# HTTP client for OSV.dev SCA queries
requests>=2.31.0

# CRT extra is required to use profiles created by `aws login`
# (the login credential provider). Without it, boto3 raises a
# "Missing Dependency" error when reading such a profile.
botocore[crt]>=1.34.0

# AST parsing (multi-language). Core runtime + per-language grammars.
# Grammars are optional at runtime: analyze_structure falls back to regex
# heuristics when a grammar is not installed.
tree-sitter>=0.23.0
tree-sitter-python>=0.23.0
tree-sitter-javascript>=0.23.0
